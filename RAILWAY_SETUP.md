# Настройка Railway проекта

## ✅ Выполнено

1. ✅ Создан базовый коммит с NestJS + Prisma
2. ✅ Проект связан с Railway (HH parser)
3. ✅ Создан сервис hh-api
4. ✅ Развернуты Postgres и Redis (через шаблоны)
5. ✅ Настроены переменные окружения для HH API:
   - `HH_CLIENT_ID`
   - `HH_CLIENT_SECRET`
   - `HH_REDIRECT_URI`
   - `HH_ACCESS_TOKEN`
   - `HH_REFRESH_TOKEN`
   - `USER_AGENT`
   - `TOPIC_QUERY`

## ⚠️ Требуется ручное действие

### Удаление лишних сервисов

Railway CLI не поддерживает удаление сервисов напрямую. Выполните через Dashboard:

1. Откройте [Railway Dashboard](https://railway.com/project/f40aac02-bab3-412e-a39f-318408c3508b)
2. Найдите все Postgres сервисы
3. Удалите лишние (оставьте один, например, самый первый созданный)
4. Найдите все Redis сервисы  
5. Удалите лишние (оставьте один)
6. Railway автоматически создаст `DATABASE_URL` и `REDIS_URL` для сервиса `hh-api`

### Проверка переменных

После удаления лишних сервисов проверьте переменные:

```bash
npx @railway/cli variables --service hh-api
```

Должны появиться:
- `DATABASE_URL` - из Postgres сервиса
- `REDIS_URL` - из Redis сервиса (если используется)

## 🚀 Деплой

После настройки переменных приложение автоматически перезапустится и применит миграции Prisma.

Проверить логи:

```bash
npx @railway/cli logs --service hh-api
```

## 📝 Дополнительные команды

```bash
# Просмотр статуса проекта
npx @railway/cli status

# Просмотр переменных
npx @railway/cli variables --service hh-api

# Просмотр логов
npx @railway/cli logs --service hh-api

# Развертывание изменений
npx @railway/cli up

# Открыть Dashboard
npx @railway/cli open
```
